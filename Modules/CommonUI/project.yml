name: CommonUI
options:
  bundleIdPrefix: com.demo

targets:
  CommonUI:
    type: framework
    platform: iOS
    deploymentTarget: "17.0"
    sources: [Sources]
    settings:
      GENERATE_INFOPLIST_FILE: YES

schemes:
  CommonUI:
    build:
      targets:
        CommonUI: all
